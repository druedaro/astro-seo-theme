---
/**
 * Header.astro — Site header with navigation and dark mode toggle
 *
 * Features:
 * - Sticky header with backdrop blur
 * - Responsive nav (mobile hamburger menu)
 * - Dark mode toggle (React Island)
 * - Language picker (i18n)
 * - Active link highlighting
 * - Megamenu support (hover on desktop, accordion on mobile)
 */
import DarkModeToggle from '../ui/DarkModeToggle.tsx';
import LanguagePicker from '../ui/LanguagePicker.astro';
import { getLangFromUrl, useTranslations, getLocalizedPath } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const currentPath = Astro.url.pathname;

// Megamenu Icons
const Icons = {
  features: `<svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>`,
  pricing: `<svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>`,
  caseStudies: `<svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>`,
  blog: `<svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>`,
  docs: `<svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>`,
};

const navItems = [
  {
    label: t('nav.product'),
    dropdown: [
      {
        title: t('nav.features'),
        description: t('nav.featuresDesc'),
        href: getLocalizedPath(lang, '/#features'),
        icon: Icons.features,
      },
      {
        title: t('nav.pricing'),
        description: t('nav.pricingDesc'),
        href: getLocalizedPath(lang, '/pricing'),
        icon: Icons.pricing,
      },
      {
        title: t('nav.caseStudies'),
        description: t('nav.caseStudiesDesc'),
        href: getLocalizedPath(lang, '/case-studies'),
        icon: Icons.caseStudies,
      },
    ],
  },
  {
    label: t('nav.resources'),
    dropdown: [
      {
        title: t('nav.blog'),
        description: t('nav.blogDesc'),
        href: getLocalizedPath(lang, '/blog'),
        icon: Icons.blog,
      },
      {
        title: t('nav.docs'),
        description: t('nav.docsDesc'),
        href: getLocalizedPath(lang, '/docs'),
        icon: Icons.docs,
      },
    ],
  },
  { label: t('nav.about'), href: getLocalizedPath(lang, '/about') },
  { label: t('nav.contact'), href: getLocalizedPath(lang, '/contact') },
];
---

<header class="sticky top-0 z-50 w-full border-b border-[var(--border)] bg-[var(--bg)]/80 backdrop-blur-lg">
  <div class="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 md:px-6">
    <!-- Logo -->
    <a
      href={getLocalizedPath(lang, '/')}
      class="flex items-center gap-2.5 font-display text-lg font-bold tracking-tight"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        class="h-7 w-7 text-[var(--color-primary-500)] dark:text-[var(--color-primary-400)]"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
      >
        <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" />
      </svg>
      <span>
        Astro
        <span class="text-[var(--color-primary-600)] dark:text-[var(--color-primary-400)]">
          SEO
        </span>
      </span>
    </a>

    <!-- Desktop Navigation -->
    <nav class="hidden md:flex items-center gap-2" aria-label="Main navigation">
      {navItems.map((item) =>
        item.dropdown ? (
          <div class="group relative inline-block">
            <button
              class="inline-flex items-center gap-1 rounded-lg px-3 py-2 text-sm font-medium text-[var(--text-secondary)] transition-colors hover:bg-[var(--bg-secondary)] hover:text-[var(--text-primary)] focus:outline-none"
              aria-haspopup="true"
              aria-expanded="false"
            >
              {item.label}
              <svg
                xmlns="http://www.w3.org/2000/svg"
                class="h-4 w-4 opacity-70 transition-transform group-hover:rotate-180"
                viewBox="0 0 20 20"
                fill="currentColor"
              >
                <path
                  fill-rule="evenodd"
                  d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z"
                  clip-rule="evenodd"
                />
              </svg>
            </button>
            <div class="invisible absolute left-0 top-full pt-2 opacity-0 transition-all duration-200 group-hover:visible group-hover:opacity-100 z-50">
              <div class="w-80 rounded-xl border border-[var(--border)] bg-[var(--bg)] p-3 shadow-xl ring-1 ring-black/5">
                <div class="flex flex-col gap-1">
                  {item.dropdown.map((subItem) => (
                    <a
                      href={subItem.href}
                      class="flex items-start gap-3 rounded-lg p-3 transition-colors hover:bg-[var(--bg-secondary)]"
                    >
                      <div class="mt-0.5 flex h-8 w-8 shrink-0 items-center justify-center rounded-md bg-[var(--color-primary-50)] text-[var(--color-primary-500)] dark:bg-[var(--color-primary-500)]/10 dark:text-[var(--color-primary-400)]">
                        <Fragment set:html={subItem.icon} />
                      </div>
                      <div>
                        <div class="text-sm font-semibold text-[var(--text-primary)]">
                          {subItem.title}
                        </div>
                        <div class="mt-1 text-xs text-[var(--text-secondary)]">
                          {subItem.description}
                        </div>
                      </div>
                    </a>
                  ))}
                </div>
              </div>
            </div>
          </div>
        ) : (
          <a
            href={item.href}
            class:list={[
              'rounded-lg px-3 py-2 text-sm font-medium transition-colors',
              currentPath === item.href || currentPath.startsWith(item.href + '/')
                ? 'text-[var(--text-primary)] bg-[var(--bg-secondary)]'
                : 'text-[var(--text-secondary)] hover:text-[var(--text-primary)] hover:bg-[var(--bg-secondary)]',
            ]}
          >
            {item.label}
          </a>
        )
      )}
    </nav>

    <!-- Right side: CTA + Dark mode + Language -->
    <div class="flex items-center gap-2 md:gap-3">
      <LanguagePicker />
      <DarkModeToggle client:load />
      <a
        href="https://github.com/druedaro/astro-seo-theme"
        target="_blank"
        rel="noopener noreferrer"
        class="hidden rounded-lg bg-[var(--color-primary-600)] px-4 py-2 text-sm font-semibold text-white transition-colors hover:bg-[var(--color-primary-700)] sm:inline-flex"
      >
        GitHub
      </a>

      <!-- Mobile menu button -->
      <button
        id="mobile-menu-btn"
        class="flex h-9 w-9 items-center justify-center rounded-lg text-[var(--text-secondary)] transition-colors hover:bg-[var(--bg-secondary)] md:hidden"
        aria-label="Open menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-5 w-5"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
          stroke-width="2"
        >
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile Navigation -->
  <nav
    id="mobile-menu"
    class="hidden border-t border-[var(--border)] px-6 py-4 md:hidden overflow-y-auto max-h-[calc(100vh-4rem)]"
    aria-label="Mobile navigation"
  >
    <div class="flex flex-col gap-2">
      {navItems.map((item, idx) =>
        item.dropdown ? (
          <div class="mobile-dropdown group border-b border-[var(--border)] pb-2 last:border-0 last:pb-0">
            <button
              class="mobile-dropdown-btn flex w-full items-center justify-between rounded-lg px-3 py-2.5 text-sm font-medium text-[var(--text-secondary)] transition-colors hover:bg-[var(--bg-secondary)] focus:outline-none"
              aria-expanded="false"
              aria-controls={`mobile-dropdown-panel-${idx}`}
            >
              {item.label}
              <svg
                xmlns="http://www.w3.org/2000/svg"
                class="h-4 w-4 opacity-70 transition-transform duration-200"
                viewBox="0 0 20 20"
                fill="currentColor"
              >
                <path
                  fill-rule="evenodd"
                  d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z"
                  clip-rule="evenodd"
                />
              </svg>
            </button>
            <div
              id={`mobile-dropdown-panel-${idx}`}
              class="mobile-dropdown-panel hidden flex-col pl-4 mt-1 space-y-1"
            >
              {item.dropdown.map((subItem) => (
                <a
                  href={subItem.href}
                  class="block rounded-lg px-3 py-2 text-sm text-[var(--text-secondary)] hover:bg-[var(--bg-secondary)] hover:text-[var(--text-primary)] transition-colors"
                >
                  {subItem.title}
                </a>
              ))}
            </div>
          </div>
        ) : (
          <a
            href={item.href}
            class:list={[
              'block rounded-lg px-3 py-2.5 text-sm font-medium transition-colors border-b border-[var(--border)] last:border-0',
              currentPath === item.href || currentPath.startsWith(item.href + '/')
                ? 'text-[var(--text-primary)] bg-[var(--bg-secondary)]'
                : 'text-[var(--text-secondary)] hover:text-[var(--text-primary)] hover:bg-[var(--bg-secondary)]',
            ]}
          >
            {item.label}
          </a>
        )
      )}
    </div>
  </nav>
</header>

<script>
  // Mobile Menu Toggle
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');

  btn?.addEventListener('click', () => {
    const isOpen = menu?.classList.toggle('hidden') === false;
    btn.setAttribute('aria-expanded', String(isOpen));
  });

  // Mobile Dropdown Accordion
  const dropdownBtns = document.querySelectorAll('.mobile-dropdown-btn');
  dropdownBtns.forEach((btn) => {
    btn.addEventListener('click', (e) => {
      const panel = btn.nextElementSibling;
      const icon = btn.querySelector('svg');
      const isExpanded = btn.getAttribute('aria-expanded') === 'true';

      if (panel && icon) {
        if (isExpanded) {
          panel.classList.add('hidden');
          panel.classList.remove('flex');
          icon.classList.remove('rotate-180');
          btn.setAttribute('aria-expanded', 'false');
        } else {
          panel.classList.remove('hidden');
          panel.classList.add('flex');
          icon.classList.add('rotate-180');
          btn.setAttribute('aria-expanded', 'true');
        }
      }
    });
  });
</script>
