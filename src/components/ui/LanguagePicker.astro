---
/**
 * LanguagePicker.astro — Accessible language switcher dropdown.
 * Renders a native <select> styled to match the design system.
 * Zero JavaScript overhead (just a native form element).
 */
import { languages, type Lang } from '../../i18n/ui';
import { getLangFromUrl, getAlternateLocales } from '../../i18n/utils';

const currentLang = getLangFromUrl(Astro.url);
const alternates = getAlternateLocales(Astro.url.pathname);
---

<div class="relative">
  <label for="language-select" class="sr-only">
    Select language
  </label>
  <select
    id="language-select"
    class="appearance-none rounded-lg border border-[var(--border)] bg-[var(--bg-secondary)] px-3 py-1.5 pr-8 text-sm font-medium text-[var(--text-secondary)] transition-colors hover:text-[var(--text-primary)] focus:border-[var(--color-primary-500)] focus:outline-none focus:ring-1 focus:ring-[var(--color-primary-500)]"
  >
    {alternates.map(({ lang, href }) => (
      <option value={href} selected={lang === currentLang}>
        {languages[lang]}
      </option>
    ))}
  </select>
  <!-- Dropdown arrow -->
  <svg
    class="pointer-events-none absolute right-2 top-1/2 h-4 w-4 -translate-y-1/2 text-[var(--text-tertiary)]"
    fill="none"
    viewBox="0 0 24 24"
    stroke="currentColor"
    stroke-width="2"
  >
    <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
  </svg>
</div>

<script>
  const select = document.getElementById('language-select') as HTMLSelectElement | null;
  select?.addEventListener('change', () => {
    window.location.pathname = select.value;
  });
</script>
